package meinStudium0;

public class ub0 {
/*public static void main(String[] args) {
        int day = 1;
        double temperature = 23.456;
        String city = "Berlin";

        // Using System.out.printf to print formatted output
        System.out.printf("Day %d: %.2f°C in %s%n", day, temperature, city);

        // Using String.format to create a formatted string
        String formattedString = String.format("Day %d: %.2f°C in %s%n", day, temperature, city);
        System.out.print(formattedString);

        // Using System.out.format (similar to printf)
        System.out.format("Day %d: %.2f°C in %s%n", day, temperature, city);
    }
        Using System.out.printf:
        Day %d: %.2f°C in %s%n is the format string.
        %d is replaced by the integer day.
        %.2f is replaced by the floating-point number temperature, formatted to two decimal places.
        %s is replaced by the string city.
        %n inserts a platform-specific newline.

    Using String.format:
        Similar to printf, but instead of printing, it returns a formatted string.
        The returned string is stored in the formattedString variable and printed with System.out.print.

    Using System.out.format:
        Similar to printf, used for formatted output.

When you run this code, the output will be:
Day 1: 23.46°C in Berlin
 */
	
    public static void main(String[] args) {
    	String s1 = "Rain";
    	 String s2 = "In";
    	 String s3 = s1 + s2 + "Spain";

    	 System.out.printf("%s %s %s%n",s1, s2, s3 );    //write your code here
    }
	 }
